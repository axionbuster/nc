module Language.NC.Parse.Expr where

import Prelude (Eq, Show)

-- | Currently only a placeholder.
--
-- Use 'error' or 'undefined' to \"instantiate\" this type
-- for now.
data Expr deriving (Show, Eq)
